{
  "id": "mock-none-api",
  "name": "Mock no-auth API",
  "baseUrl": "https://mock.invalid",
  "timeoutMs": 120000,
  "authType": "none",
  "keyConfig": {},
  "actions": [
    {
      "id": "mock",
      "name": "Mock",
      "url": "/v1/mock",
      "method": "POST",
      "description": "No authentication mock",
      "request": { "prompt": "{{value}}" },
      "response": {
        "candidates": [{ "content": { "parts": [{ "text": "{{value}}" }] } }]
      }
    }
  ]
}
